# To-do-List
To-do List made with Python version 3.12 and HTML

Before running the index.html file needs to be moved into a folder called templates in the same repository as the server.

Once you run the server file you can open the server from the link and use the To-do List.
